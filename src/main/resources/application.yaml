spring:
  application:
    name: school-bot
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

telegram:
  bot:
    name: МАОУ СОШ №5 Расписание
    token: ${TELEGRAM_BOT_TOKEN}